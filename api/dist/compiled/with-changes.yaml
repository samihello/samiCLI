openapi: 3.0.0
info:
  title: Sami API
  version: 0.1.0
servers:
  - url: https://api.sample.com/v1
paths:
  /users:
    get:
      summary: Get users
      responses:
        '200':
          description: JSON array of users
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string